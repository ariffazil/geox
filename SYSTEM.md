# SYSTEM: The Unified Runtime & Execution Engine

**Canon ID:** `ARIF-OS::SYSTEM::400-700::v1.0`  
**Zone:** `400_FORGE` to `700_OPS`  
**Status:** `OPERATIONAL · ΔΩΨ · F1-F13 ENFORCED`  
**Motto:** *Ditempa Bukan Diberi — Forged, Not Given*

---

## 🏗️ Executive Summary

`SYSTEM.md` defines the **Unified Runtime** of arifOS. it unifies the **Reality Bridge** (physical execution), **Skill Bridge** (logical orchestration), and **Execution Validator** (truth feedback) into a single-term sovereign document.

```
SYSTEM = Reality (Perception) + Skill (Action) + Validator (Truth)
```

---

## 🧬 Part 1: Reality Bridge (Physical Perception)

The Reality Bridge connects AI intent to actual system state. It is the "Hands of the Kernel."

### 1.1 Tool Adapters
-   **Docker:** `ps`, `restart`, `logs`, `build`.
-   **Git:** `status`, `checkout`, `commit`, `push`.
-   **Filesystem:** `list`, `read`, `write`, `exists`.

### 1.2 Security Invariants (F12)
Every command executed through the Reality Bridge is sanitized to prevent injection (`&&`, `||`, `;`, etc.) and ensure the **F12 Injection** floor is never breached.

---

## 🛠️ Part 2: Skill Bridge (Logical Action)

The Skill Bridge is the orchestrator that manages high-level tasks (Skills) and maps them to Reality Bridge calls.

### 2.1 The Forge Cycle
1.  **Checkpoint (F1):** Create a reversibility state before execution.
2.  **Dry Run (F7):** Default mode is `dry_run=True` to simulate impact.
3.  **Operator (F13):** High-risk actions require an identified `operator` (Muhammad Arif bin Fazil).

### 2.2 Core Skills
-   **vps-docker:** Infrastructure management.
-   **git-ops:** Version control and repository hardening.
-   **security-audit:** F12/F9 compliance scanning.

---

## ⚖️ Part 3: Execution Validator (Truth Feedback)

The Validator ensures that what the system *claims* happened actually *did* happen.

### 3.1 Verification Hashes (F2)
Every execution returns a **Verification Hash**. The Validator compares this against the expected state.
-   **Success:** τ ≥ 0.99. Proceed to SEAL.
-   **Mismatch:** τ < 0.99. Trigger **SABAR** or **VOID**.

### 3.2 W³ Score (Wisdom × Will × Welfare)
Execution results are fed into the W³ matrix to determine the final **Witness Score** for the session.

---

## 🔄 Version History

| Version | Date | Changes |
|---------|------|---------|
| v1.0 | 2026-03-31 | Unified CLOSED_LOOP_SYSTEM and Bridge specs into SYSTEM.md |

---

**OPERATIONAL** ⚙️
`ΔΩΨ | 700_OPS | 888_JUDGE`  
*Ditempa Bukan Diberi*
